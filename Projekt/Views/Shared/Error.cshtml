@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="neo-card" style="text-align: center; max-width: 800px; margin: 0 auto;">
    <h1 style="font-size: 3rem; font-weight: 800; color: var(--color-primary); margin-bottom: 1rem;">
        <i class="bi bi-exclamation-triangle-fill" style="color: var(--neo-red);"></i><br />
        Oops!
    </h1>
    <h2 style="font-weight: 700; text-transform: uppercase; margin-bottom: 2rem; font-size: 1.5rem; color: var(--color-text-main);">
        An error occurred while processing your request
    </h2>

    @if (Model.ShowRequestId)
    {
        <div class="neo-inset" style="padding: 1.5rem; margin: 2rem 0; text-align: left;">
            <p style="margin: 0; font-weight: 600;">
                <span class="text-muted">Request ID:</span> 
                <code style="font-size: 1.1rem; color: var(--color-primary); margin-left: 0.5rem;">@Model.RequestId</code>
            </p>
        </div>
    }

    <a asp-controller="Home" asp-action="Index" class="neo-btn neo-btn-primary" style="margin-top: 2rem;">
        <i class="bi bi-house-fill"></i> Go to Homepage
    </a>
</div>

<div class="neo-card" style="margin-top: 2rem; max-width: 800px; margin-left: auto; margin-right: auto;">
    <h3 style="font-weight: 900; text-transform: uppercase; margin-bottom: 1rem;">
        <i class="bi bi-info-circle-fill"></i> Development Mode
    </h3>
    <p style="font-weight: 700; margin-bottom: 1rem;">
        Swapping to <strong>Development</strong> environment will display more detailed information about the error that occurred.
    </p>
    <p style="font-weight: 700; margin-bottom: 0;">
        <strong>⚠️ Warning:</strong> The Development environment shouldn't be enabled for deployed applications.
        It can result in displaying sensitive information from exceptions to end users.
    </p>
</div>
