@model Projekt.Models.Project
@{
    ViewData["Title"] = "Add Project";
}

<div class="neo-card" style="max-width: 700px; margin: 0 auto;">
    <h1 style="font-size: 2.5rem; font-weight: 900; text-transform: uppercase; margin-bottom: 2rem;">
        <i class="bi bi-kanban-fill"></i> Add New Project
    </h1>

    <form asp-action="Create" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger" style="padding: 1rem; border: 4px solid var(--neo-red); background: #FFE5E8; font-weight: 700; margin-bottom: 1.5rem;"></div>

        <div class="neo-form-group">
            <label asp-for="Name" class="neo-form-label"></label>
            <input asp-for="Name" class="neo-input" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="neo-form-group">
            <label asp-for="Description" class="neo-form-label"></label>
            <textarea asp-for="Description" class="neo-input" rows="4"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem;">
            <div class="neo-form-group">
                <label asp-for="StartDate" class="neo-form-label"></label>
                <input asp-for="StartDate" class="neo-input" type="date" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>

            <div class="neo-form-group">
                <label asp-for="EndDate" class="neo-form-label"></label>
                <input asp-for="EndDate" class="neo-input" type="date" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-top: 2rem;">
            <button type="submit" class="neo-btn neo-btn-success" style="width: 100%;">
                <i class="bi bi-check-circle"></i> Create Project
            </button>
            <a asp-action="Index" class="neo-btn neo-btn-danger" style="width: 100%; text-align: center;">
                <i class="bi bi-x-circle"></i> Cancel
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
